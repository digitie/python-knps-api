[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "python-knps-api"
version = "0.2.0"
description = "Unofficial Python client for Korea National Park Service public file datasets."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "GPL-3.0-or-later" }
authors = [
  { name = "digitie" }
]
keywords = ["korea", "national-park", "knps", "data.go.kr", "trail", "geodata"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: GNU General Public License v3 or later (GPLv3+)",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Typing :: Typed",
]
dependencies = [
  "httpx>=0.27",
  "pydantic>=2.7",
  "boto3>=1.34",
]

[project.optional-dependencies]
dev = [
  "pytest>=8",
  "pytest-asyncio>=0.23",
  "ruff>=0.5",
  "mypy>=1.10",
  "types-boto3[s3]",
]
debug-ui = [
  "pandas>=2.0",
  "pandas-stubs>=2.0",
  "streamlit>=1.35",
]
geo = [
  "pyproj>=3.6",
  "pyshp>=2.3",
]

[tool.setuptools.package-dir]
"" = "src"

[tool.setuptools.packages.find]
where = ["src"]
include = ["knps*"]

[tool.setuptools.package-data]
knps = ["py.typed"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
python_version = "3.11"
strict = true
warn_unused_ignores = true
files = ["src", "debug_ui"]

# debug_ui는 pandas/streamlit을 사용한다. stubs가 깔린 환경(debug-ui extras
# 설치)이면 자동 사용되고, 없으면 missing import 무시. 이 모듈들은 KNPS
# 코어 타입과 무관해서 strict 검사를 강요할 가치가 낮다.
[[tool.mypy.overrides]]
module = ["pandas", "pandas.*", "streamlit", "streamlit.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-ra -m 'not live'"
asyncio_mode = "auto"
markers = [
  "live: tests that call real KNPS/data.go.kr file dataset servers",
]
